<section>
  <div class="bg-gray-100 pt-5">
    <div class="container">
      <div class="max-w-2xl mx-auto">
        <h2 class="titulo mb-3">Somos mais de <span class="text-primary">7 mil usários</span> em todo Brasil!</h2>
        <p class="mb-15 text-lg text-center">Veja o que nossos clientes falam sobre o sistema WebMais.</p>
      </div>

      <div class="grid lg:grid-cols-3 gap-5">
        {% for video in videos %}
        <div>
          <a href="https://youtu.be/{{ video.link }}" class="video block relative group hover:brightness-50 transition">
            <img
              loading="lazy"
              src="{{ images }}/video/{{ video.img }}.webp"
              class="rounded-xl h-50 md:h-68 w-full object-cover"
              alt="{{ video.nome }}"
              title="{{ video.nome }}"
              width="478"
              height="272"
            />
            <svg class="icon size-10 absolute-center text-white/50" data-icon="ui:play"></svg>
          </a>
          <h3 class="font-semibold mt-4 -mb-1">{{ video.nome }}</h3>
          <p class="text-sm">{{ video.cargo }}</p>
        </div>
        {% endfor %}
      </div>

    </div>
  </div>
</section>